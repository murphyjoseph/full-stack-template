import { Text } from "@repo/ui/text";

export default function DemoPage() {
  return (
    <Text color="fg.muted">
      Select a demo from the navigation above to see different API patterns in
      action.
    </Text>
  );
}
